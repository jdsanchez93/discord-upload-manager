# AWS credentials - use a profile name from ~/.aws/credentials
AWS_PROFILE=your-profile
AWS_REGION=us-east-1

# From `cdk deploy -c stage=dev` outputs
FILES_TABLE_NAME=
WEBHOOKS_TABLE_NAME=
UPLOAD_BUCKET_NAME=
CLOUDFRONT_DOMAIN=

# Auth0 (optional for local dev - leave empty to bypass auth)
AUTH0_DOMAIN=
AUTH0_AUDIENCE=
